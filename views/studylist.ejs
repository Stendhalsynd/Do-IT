<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../static/style/css/common/header.css" />
    <title>스터디 목록</title>
  </head>
  <body>
    <header class="header">
      <img
        src="../static/images/Do-IT logo.png"
        alt="Do-IT logo"
        class="header__logo"
      />
      <div class="header__button">
        <button class="header__button--practice">연습하기</button>
        <button class="header__button--study">스터디</button>
        <button class="header__button--sign-in">로그인</button>
        <button class="header__button--point">포인트</button>
        <button class="header__button--profile">프로필</button>
        <button class="header__button--sign-out">로그아웃</button>
      </div>
    </header>
    <div id="studyList">
      <% for (let i = 0; i < list.length; i++) { %>
      <div onclick="studyDetail('<%= list[i].id %>')">
        <h4><%= list[i].title%></h4>
        <% list[i].category.forEach((elem)=> { %>
        <span><%= elem %></span>
        <% }) %>
        <br />
        <% if (list[i].memCurr >= list[i].memTotal) { %>
        <span>모집완료</span>
        <% } else { %>
        <span>모집중</span>
        <% } %>
        <span><%= list[i].memCurr %> / <%= list[i].memTotal %></span>
      </div>
      <% } %>
    </div>
    <script src="../static/script/study/studylist.js"></script>
  </body>
</html>
